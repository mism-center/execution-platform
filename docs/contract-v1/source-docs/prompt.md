The following are documents defining 3 systems.  An execution endpoint for models (discover-execution-contract.md) a kubernetes operator for execution of apps (helxapp-controller.md) and conjecture of application of  webapp called appstore to provide support for user application execution.  All 3 of these are required to work together (in fact appstore uses helxapp-controller), and it's expected that the model-executor should work in concert with appstore.  Therefore reconcile these systems with the above stated purpose.  Assume OIDC as defined in chart which will provide identity for appstore and helxuser properties.  This combined execution contract should at minimum offer all the features that are defined in discover-execution-contract.  This document should identify the underlying execution/user objects and connected them to the API.  Consider testing, and separate the document into relevant sections.  If unsure, ask questons
