# Prompt to generate the Kafka reimpl

scan the source code, from that formulate the execution model it implies, imagine the features, requirements, gaps accosicated with re-implementing to use kafka and/or using      
another language, organize into the compact modules, consider tests.  As output, generate a single cohesive document in markdown.  If unsure, ask questions. 
